/* import request from '@/router/axios';

export const getUsualList = (current, size) => {
  return request({
    url: '/api/blade-log/usual/list',
    method: 'get',
    params: {
      current,
      size
    }
  })
} */